import React from 'react';
import ProgressBar from '../../components/ProgressBar';

const StatsSummary = () => {
    return (
        <div>
            <p>Прогресс</p>
            <ProgressBar />
        </div>
    );
};

export default StatsSummary;
